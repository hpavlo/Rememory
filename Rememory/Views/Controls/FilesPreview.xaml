<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Rememory.Views.Controls.FilesPreview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:Rememory.Views.Controls"
             xmlns:tk="using:CommunityToolkit.WinUI"
             xmlns:tkconverters="using:CommunityToolkit.WinUI.Converters"
             x:Name="ParentControl"
             Loaded="ParentControl_Loaded">
    <UserControl.Resources>
        <tkconverters:EmptyObjectToObjectConverter x:Key="EmptyObjectToBoolConverter"
                                                   NotEmptyValue="True"
                                                   EmptyValue="False" />
    </UserControl.Resources>
    
    <Grid>
        <ListView x:Name="FilesListView"
                  ItemsSource="{x:Bind Files, Mode=OneWay}"
                  IsHitTestVisible="False">
            <ListView.ItemContainerTransitions>
                <TransitionCollection />
            </ListView.ItemContainerTransitions>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem"
                       BasedOn="{StaticResource DefaultListViewItemStyle}">
                    <Setter Property="MinHeight"
                            Value="16" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter x:Name="FilePresenter"
                                                  Height="20"
                                                  FontSize="{ThemeResource BodyTextBlockFontSize}">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup>
                                            <VisualState x:Name="InvalidFilePath">
                                                <VisualState.StateTriggers>
                                                    <tk:IsEqualStateTrigger Value="{Binding IsPathCorrect}"
                                                                            To="False" />
                                                </VisualState.StateTriggers>
                                                <VisualState.Setters>
                                                    <Setter Target="FilePresenter.Opacity"
                                                            Value="0.5" />
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>

                                        <VisualStateGroup>
                                            <VisualState x:Name="CompactMode">
                                                <VisualState.StateTriggers>
                                                    <StateTrigger IsActive="{Binding ShowInCompactMode}" />
                                                </VisualState.StateTriggers>
                                                <VisualState.Setters>
                                                    <Setter Target="FilePresenter.Height"
                                                            Value="16" />
                                                    <Setter Target="FilePresenter.FontSize"
                                                            Value="{ThemeResource CaptionTextBlockFontSize}" />
                                                </VisualState.Setters>
                                            </VisualState>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                </ContentPresenter>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:FilePreviewModel"
                              x:DefaultBindMode="OneWay">
                    <Grid x:Name="RootGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0"
                               Width="16"
                               Height="16"
                               Margin="0,0,8,0"
                               Source="{x:Bind ImageSource}" />

                        <TextBlock Grid.Column="1"
                                   Text="{x:Bind Name}"
                                   TextTrimming="CharacterEllipsis" />

                        <TextBlock x:Name="RightSizeText"
                                   x:Load="{x:Bind RightSideInfo, Converter={StaticResource EmptyObjectToBoolConverter}}"
                                   Grid.Column="2"
                                   Margin="8,0,0,0"
                                   Text="{x:Bind RightSideInfo}"
                                   Opacity="0.5" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
