using Microsoft.UI.Xaml;

namespace Rememory.Themes
{
    // Using code behind for DataTemplates resources to allow use x:Bind in the DataTemplates.xaml file
    public sealed partial class DataTemplates : ResourceDictionary
    {
        public DataTemplates()
        {
            InitializeComponent();
        }
    }
}
